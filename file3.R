x1 = 3
x2 <- 3
#diff ways of assigning value
x1
x2

ls() #variables in the env
women
?AirPassengers
data() #pre available data sets for use
AirPassengers

library() #packages loaded

?mean #help
x <- c(0:10, 50)
x
?c
length(x)
xm <- mean(x)
c(xm, mean(x, trim = 0.10))
??mean #search mean from other sources where people could have created mean with different functionalities
1:10000000

getwd() #working directory
plot(1:10) #basic command to plot
 